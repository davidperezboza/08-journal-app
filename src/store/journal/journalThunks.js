export const startNewNote = () => {
  return async(dispatch) => {
    //uid del usiario

    const newNote = {
      title: '',
      body: '',
      date: new Date().getTime(),
    };

    // dispatch
    // dispacth(NewNote)
    // dispatch(activarNote)
  };
};